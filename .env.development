# ============================================================
# FILE: .env.example (Production-ready)
# ============================================================

# -----------------------------
# Application
# -----------------------------
NODE_ENV=development                  # 'production' or 'development' or 'test'
PORT=5000                            # Change to your desired production port
APP_NAME=User Management System
APP_URL=https://yourdomain.com       # Use your real domain in production

# -----------------------------
# Database (MongoDB)
# -----------------------------
DATABASE_URL=mongodb+srv://noyonsaha001:nmnjwIca54FXEmGO@cluster0.foguvuz.mongodb.net/OOPBasedBackend?retryWrites=true&w=majority&appName=Cluster0
# Use a proper MongoDB Atlas connection string in production with strong credentials

# -----------------------------
# Security
# -----------------------------
JWT_SECRET=replace-with-strong-random-string
JWT_EXPIRES_IN=1d                    # Short-lived access token
JWT_REFRESH_SECRET=replace-with-another-strong-random-string
JWT_REFRESH_EXPIRES_IN=30d           # Refresh token valid for 30 days
PEPPER_SECRET=replace-with-random-string-for-argon2

# -----------------------------
# Logging
# -----------------------------
LOG_LEVEL=info                        # 'error', 'warn', 'info', 'debug' for dev

# -----------------------------
# Rate Limiting
# -----------------------------
RATE_LIMIT_WINDOW_MS=900000          # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100          # Max requests per window per IP

# -----------------------------
# CORS
# -----------------------------
CORS_ORIGIN=https://yourdomain.com   # Production domain; multiple origins can be comma-separated
